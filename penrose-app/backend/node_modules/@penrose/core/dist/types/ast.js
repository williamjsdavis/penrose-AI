import { isConcrete } from "../engine/EngineUtils.js";
// metaObj causes TypeScript to enforce that metaProps is correct
const metaObj = {
    nodeType: undefined,
    start: undefined,
    end: undefined,
};
export const metaProps = Object.keys(metaObj);
//#endregion
export const location = (node) => {
    if (isConcrete(node)) {
        return {
            start: node.start,
            end: node.end,
            nodeType: node.nodeType,
        };
    }
    else
        return {
            nodeType: node.nodeType,
        };
};
//# sourceMappingURL=ast.js.map