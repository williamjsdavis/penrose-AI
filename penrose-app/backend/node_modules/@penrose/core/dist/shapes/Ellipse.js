import { boolV, floatV, noPaint, strV } from "../utils/Util.js";
import { sampleColor, sampleHeight, sampleVector, sampleWidth, } from "./Samplers.js";
export const sampleEllipse = (context, canvas) => ({
    name: strV("defaultEllipse"),
    strokeWidth: floatV(0),
    strokeStyle: strV("solid"),
    strokeColor: noPaint(),
    strokeDasharray: strV(""),
    fillColor: sampleColor(context),
    center: sampleVector(context, canvas),
    rx: sampleWidth(context, canvas),
    ry: sampleHeight(context, canvas),
    ensureOnCanvas: boolV(true),
});
export const makeEllipse = (context, canvas, properties) => ({
    ...sampleEllipse(context, canvas),
    ...properties,
    shapeType: "Ellipse",
    passthrough: new Map(),
});
//# sourceMappingURL=Ellipse.js.map