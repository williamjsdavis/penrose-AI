import { boolV, floatV, noPaint, ptListV, strV } from "../utils/Util.js";
import { sampleColor } from "./Samplers.js";
export const samplePolygon = (context, _canvas) => ({
    name: strV("defaultPolygon"),
    strokeWidth: floatV(0),
    strokeStyle: strV("solid"),
    strokeColor: noPaint(),
    strokeDasharray: strV(""),
    fillColor: sampleColor(context),
    scale: floatV(1),
    points: ptListV([
        [0, 0],
        [0, 10],
        [10, 0],
    ]),
    ensureOnCanvas: boolV(true),
});
export const makePolygon = (context, canvas, properties) => ({
    ...samplePolygon(context, canvas),
    ...properties,
    shapeType: "Polygon",
    passthrough: new Map(),
});
//# sourceMappingURL=Polygon.js.map