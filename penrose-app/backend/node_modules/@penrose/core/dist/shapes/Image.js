import { boolV, floatV, strV } from "../utils/Util.js";
import { sampleHeight, sampleVector, sampleWidth, } from "./Samplers.js";
export const sampleImage = (context, canvas) => ({
    name: strV("defaultImage"),
    center: sampleVector(context, canvas),
    width: sampleWidth(context, canvas),
    height: sampleHeight(context, canvas),
    rotation: floatV(0),
    href: strV("defaultImage"),
    ensureOnCanvas: boolV(true),
    preserveAspectRatio: strV(""),
});
export const makeImage = (context, canvas, properties) => ({
    ...sampleImage(context, canvas),
    ...properties,
    shapeType: "Image",
    passthrough: new Map(),
});
//# sourceMappingURL=Image.js.map