import { boolV, floatV, noPaint, strV } from "../utils/Util.js";
import { sampleColor, sampleVector, sampleWidth, } from "./Samplers.js";
export const sampleCircle = (context, canvas) => {
    return {
        name: strV("defaultCircle"),
        strokeWidth: floatV(0),
        strokeStyle: strV("solid"),
        strokeColor: noPaint(),
        strokeDasharray: strV(""),
        fillColor: sampleColor(context),
        center: sampleVector(context, canvas),
        r: sampleWidth(context, canvas),
        ensureOnCanvas: boolV(true),
    };
};
export const makeCircle = (context, canvas, properties) => ({
    ...sampleCircle(context, canvas),
    ...properties,
    shapeType: "Circle",
    passthrough: new Map(),
});
//# sourceMappingURL=Circle.js.map