import { CircleProps } from "../shapes/Circle.js";
import { EllipseProps } from "../shapes/Ellipse.js";
import { LineProps } from "../shapes/Line.js";
import { PathProps } from "../shapes/Path.js";
import { RectangleProps } from "../shapes/Rectangle.js";
import * as ad from "../types/ad.js";
import { Center, Poly, Rect, Rotate, Scale } from "../types/shapes.js";
export interface BBox {
    width: ad.Num;
    height: ad.Num;
    center: ad.Pt2;
}
export interface Corners {
    topRight: ad.Pt2;
    topLeft: ad.Pt2;
    bottomLeft: ad.Pt2;
    bottomRight: ad.Pt2;
}
export interface Intervals {
    xRange: [ad.Num, ad.Num];
    yRange: [ad.Num, ad.Num];
}
export interface Edges {
    top: [ad.Pt2, ad.Pt2];
    bot: [ad.Pt2, ad.Pt2];
    left: [ad.Pt2, ad.Pt2];
    right: [ad.Pt2, ad.Pt2];
}
/**
 * Input: A width, height, and center.
 * Output: A new BBox.
 */
export declare const bbox: (width: ad.Num, height: ad.Num, center: ad.Pt2) => BBox;
export declare const corners: (b: BBox) => Corners;
/**
 * Input: A BBox and an inflation parameter delta.
 * Output: A BBox inflated on all sides by delta.
 */
export declare const inflate: (b: BBox, delta: ad.Num) => BBox;
/**
 * Input: A BBox.
 * Output: The min X of the BBox.
 */
export declare const minX: (b: BBox) => ad.Num;
/**
 * Input: A BBox.
 * Output: The max X of the BBox.
 */
export declare const maxX: (b: BBox) => ad.Num;
/**
 * Input: A BBox.
 * Output: The min Y of the BBox.
 */
export declare const minY: (b: BBox) => ad.Num;
/**
 * Input: A BBox.
 * Output: The max Y of the BBox.
 */
export declare const maxY: (b: BBox) => ad.Num;
/**
 * Input: A BBox.
 * Output: The X interval of the BBox.
 */
export declare const xRange: (b: BBox) => [ad.Num, ad.Num];
/**
 * Input: A BBox.
 * Output: The Y interval of the BBox.
 */
export declare const yRange: (b: BBox) => [ad.Num, ad.Num];
/**
 * Input: A BBox.
 * The four edges of the BBox.
 */
export declare const edges: (b: BBox) => Edges;
export declare const bboxFromPoints: (points: ad.Pt2[]) => BBox;
export declare const bboxFromRotatedRect: (center: ad.Pt2, w: ad.Num, h: ad.Num, clockwise: ad.Num, strokeWidth: ad.Num) => BBox;
export declare const bboxFromCircle: ({ r, center, strokeWidth, }: CircleProps<ad.Num>) => BBox;
export declare const bboxFromEllipse: ({ rx, ry, center, strokeWidth, }: EllipseProps<ad.Num>) => BBox;
export declare const bboxFromRect: ({ width, height, center, strokeWidth, rotation, }: RectangleProps<ad.Num>) => BBox;
export declare const bboxFromRectlike: ({ center, width, height, rotation, }: Center<ad.Num> & Rect<ad.Num> & Rotate<ad.Num>) => BBox;
export declare const bboxFromPolygon: ({ points, scale, }: Poly<ad.Num> & Scale<ad.Num>) => BBox;
export declare const bboxFromLinelike: ({ start, end, strokeWidth, }: LineProps<ad.Num>) => BBox;
export declare const bboxFromPath: ({ d }: PathProps<ad.Num>) => BBox;
export declare const intersectBbox: (bbox1: BBox, bbox2: BBox) => BBox;
//# sourceMappingURL=BBox.d.ts.map